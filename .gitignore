# ── Secretos — NUNCA commitear ──────────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key
gemelo-digital-backend/certs/
gemelo-digital-backend/keys/
gemelo-digital-backend/app/keys/

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Alembic — solo ignorar cache, NO las migraciones ─────────────────────────
# (las migraciones en alembic/versions/ SÍ se commitean)

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Logs / cobertura ─────────────────────────────────────────────────────────
*.log
.coverage
htmlcov/

# Archivos temporales de operaciones AWS / CloudFront / git
cf-config-tmp.json
cf-dist.json
cf-update.json
commits.json

# Build artifacts del generador de documentos
docs/build/node_modules/
docs/build/package-lock.json

# Sesiones persistidas en dev local (tokens Brightspace — NUNCA commitear)
.session_store.json
.session_store.json.tmp
gemelo-digital-backend/.session_store.json
gemelo-digital-backend/.session_store.json.tmp

# Directorios de Claude Code (sesiones/worktrees locales)
.claude/

# Datos de runtime (best-effort en disco — NO commitear; efímeros en ECS)
known_users.json
announcements.json
announcements.json.tmp
bug_reports.jsonl
gemelo-digital-backend/known_users.json
gemelo-digital-backend/announcements.json
gemelo-digital-backend/announcements.json.tmp
gemelo-digital-backend/bug_reports.jsonl
svc2.json
